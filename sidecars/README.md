# sidecars

Static `ffmpeg` and `ffprobe` builds for arm64 macOS live here (git-ignored).
Fetch with `scripts/get-ffmpeg.sh`. Must be an **LGPL** build (no `--enable-gpl`).
Pinned sha256 values go in `scripts/get-ffmpeg.sh` once chosen (Task 4).
