[package]
name = "spike"
version = "0.0.0"
edition = "2021"
publish = false

# Task 0 — pin the versions that actually build on the Mac, then record them in docs/versions.md.
# Cursor: check crates.io for the current release of each before filling in.
[dependencies]
anyhow = "1"
# ort = { version = "<FILL>", features = ["coreml"] }
# whisper-rs = { version = "<FILL>", features = ["metal"] }
# image = "0.25"
# ndarray = "0.16"
