# TASK-012b: First-run + Library screens
Agent: Cursor on the Mac. Branch: task/12b-library. Depends: 012a. UX spec: docs/ux-spec.md.

## Acceptance
- [ ] Fresh data dir → first-run screen shows download progress per file, retry on failure, then proceeds
- [ ] Library: Add Folder (native picker + drag-drop), list of videos with status pill and progress bar, per-video error text expandable, Cancel button, Re-index
- [ ] Indexing a fixture folder shows live progress; cancel works
- [ ] Empty state text per blueprint §7
