# Fixtures

3–5 clips John owns, ≤ 30 s each, ≤ 20 MB total. Needed before Task 3.
Suggested mix: one with hard cuts, one with a slow pan and no cuts, one vertical phone clip, one with clear speech, one with no audio.
`golden/` holds the reference outputs; never edit by hand.
